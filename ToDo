
- make a smart way to pre-load pages
    + https://github.com/turbolinks/turbolinks
    + https://github.com/ftlabs/fastclick
    + https://github.com/dieulot/instantclick/
    + pjax https://github.com/defunkt/jquery-pjax

- need hot reload
    + could use https://www.npmjs.com/package/light-server to hot reload
    + compile only necessary

- typescript

- comment can be a pb
"use strict";require("./index.css"),console.log("Hello welcome");import { info } from 'logol';import { error } from 'logol'; // import { getUptime } from '../api/uptime';import { log } from 'logol';window.require = require;(window.r_ka || []).forEach(function(fn) { fn(); });

- on error shell parcel does not throw error, maybe need to check exit code?
    when type in api file

- FIX windows:
    - use npx.cmd
    - use cross-spawn
    - use url-join

- deploy: zeit (server function), heroku, netlify, aws?

- how to handle asset
